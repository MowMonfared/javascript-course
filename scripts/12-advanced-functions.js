/*
setTimeout(function () {
  console.log('timeout');
}, 3000);
*/

['good', 'bad', 'ugly'].forEach(function (value, index) {
  console.log(index);
  console.log(value);
});

const colors = ['red', 'blue', 'yellow'];
colors.forEach(function (value) {
  console.log(value);
});
